export const upperCase = (str) => str.toUpperCase();

export const lowerCase = (str) => str.toLowerCase();

export const replace = (str, targetValue, newValue) => str.replace(targetValue, newValue);

export const replaceAll = (str, targetValue, newValue) => str.replaceAll(targetValue, newValue);
